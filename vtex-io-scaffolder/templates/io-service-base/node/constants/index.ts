/**
 * Shared constants live here. Only add a constant when the value is used in
 * more than one place; one-off magic values belong next to the code that
 * uses them.
 */
export const DEFAULT_TIMEOUT_MS = 10000
